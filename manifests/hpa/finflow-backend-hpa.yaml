apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata: 
  name: hpa-backend
  namespace: finflow
spec:
  scaleTargetRef:
     apiVersion: apps/v1
     kind: Deployment
     name: finflow-backend
  minReplicas: 1
  maxReplicas: 3
  metrics:
    - type: Resource
      resource:
         name: cpu
         target:
           type: Utilization
           averageUtilization: 70 
