apiVersion: v1
kind: Service
metadata: 
  name: finflow-landing-svc
  namespace: finflow
spec: 
  selector:
    app: finflow-landing
  ports:
   - protocol: TCP
     port: 80
     targetPort: 80
  type: ClusterIP
