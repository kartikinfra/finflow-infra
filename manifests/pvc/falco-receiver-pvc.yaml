apiVersion: v1
kind: PersistentVolumeClaim
metadata: 
  name: falco-receiver-pvc
  namespace: monitoring
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 500Mi
